extends ./base

block header
    title Success

block content
    div(class="alert alert-success" role="alert" style={
        "margin-top": "30px",
        "padding": "30px",
        "font-size": "20px",
        "text-align": "center"
    }) operate success!

    script.
        setTimeout(function () {
            window.location = '/';
        }, 3000);